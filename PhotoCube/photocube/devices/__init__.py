from keyboard import KeyboardHandler
from mouse import MouseHandler

# Create instance of the keyboard handler.
keyboard_handler = KeyboardHandler()

# Create instance of the mouse handler.
mouse_handler = MouseHandler()
